# Pointer — Mission Until Limits

**Canonical plan (only):** [jev-roadmap.md](jev-roadmap.md) — Track B `HUL-*` (mission record, dual budget, Jev scope gate, findings, resume, product phases A–D).

**Short Freebuff paste:** [jev-mission-prompt.md](jev-mission-prompt.md)

This file is **not** a plan. It holds no STATUS, DoD, or schedule.  
If anything here ever disagrees with `docs/jev-roadmap.md`, **the canon wins**.

### One-line summary of HUL (for readers who land here)

Any operator request can run as a **mission pack** under `missions/<id>/` with explicit `success_definition`, cost/token limits, and a **terminal reserve** used only for `FINDINGS.md`. Attempts continue until success (verifier if any) or working-budget/API/token limits or stall — never because the model “feels done.” Jev typed judgments (when policy + key exist) block false complete against scope. Full receipts + resume pack every step.
