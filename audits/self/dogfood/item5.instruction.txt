In run_bench, fail manifest schema errors cleanly: at the top of the per-task loop set name = task.get('name') or 'task' (mirroring load_manifest's default) and use it for the running/done log lines and task_id; after the TaskSandbox restore, read instruction = task.get('instruction') and raise HarnessError(f'bench task {name} is missing required key instruction') when it is missing or empty; pass the validated instruction variable into apply_edit. Keep every other default, the HarnessError-per-task catch around apply_edit, the finally-restore, and summarize exactly as they are. Touch nothing else.